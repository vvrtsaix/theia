name: theia

services:
  postgres:
    image: postgres:18-alpine
    container_name: theia-pg
    environment:
      POSTGRES_USER: theia
      POSTGRES_PASSWORD: theia
      POSTGRES_DB: theia
      # `uuidv7()` is built into Pg 18 — no extension needed.
    ports:
      - "5432:5432"
    volumes:
      # Pg18 images store data under /var/lib/postgresql/<major>/docker/...
      # Mount the parent so pg_upgrade --link works in-place on future bumps.
      - pgdata:/var/lib/postgresql
      - ./00-bootstrap.sql:/docker-entrypoint-initdb.d/00-bootstrap.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U theia -d theia"]
      interval: 2s
      timeout: 2s
      retries: 20

  otel-collector:
    # Bumped after 0.115.0 was pulled from Docker Hub.
    image: otel/opentelemetry-collector-contrib:0.130.0
    container_name: theia-otel
    command: ["--config=/etc/otel-collector.yaml"]
    volumes:
      - ./otel-collector.yaml:/etc/otel-collector.yaml:ro
    ports:
      - "4317:4317"   # OTLP gRPC
      - "4318:4318"   # OTLP HTTP
    depends_on:
      jaeger:
        condition: service_started

  jaeger:
    # Jaeger v1 all-in-one with native OTLP receivers. Tag bumped after
    # 1.62 was pulled from Docker Hub — pin to the current v1 line.
    image: jaegertracing/all-in-one:1.65.0
    container_name: theia-jaeger
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "16686:16686"   # UI
      - "14250:14250"   # gRPC

volumes:
  pgdata:
