# CLAUDE.md

Guidance for Claude Code lives in **[`AGENT.md`](./AGENT.md)** — the vendor-agnostic source of truth shared with all AI coding agents (Claude Code, Cursor, Copilot, Aider, etc.).

Read `AGENT.md` for: project overview, commands, stack constraints, architecture (contract-first, RLS, cluster/actor, RPC), conventions (Drizzle, subpath imports, v4 Schema gotchas, Kobalte + modular-forms), and current phase status.

Read `ARCHITECTURE.md` before non-trivial changes — locked decisions, package map, 10-phase plan.

Do not duplicate content here. Update `AGENT.md` instead.
